<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <uses-permission android:name="android.permission.CAMERA"/>
    <!-- The lock in Settings is the phone's own authentication. The biometric
         plugin declares this too; it is repeated here so that a reader of the
         manifest can see everything Arcanum asks the system for in one place. -->
    <uses-permission android:name="android.permission.USE_BIOMETRIC"/>
    <uses-feature android:name="android.hardware.camera" android:required="false"/>

    <!--
      Cleartext is permitted because the one plain-HTTP address Arcanum talks to
      is a service the user runs themselves on their own private network - the
      companion price-history server, reached over Tailscale or the LAN.

      Android blocks cleartext by default from targetSdk 28 onwards, and a
      network security config cannot express "private address ranges only"
      because it matches domains and literals, not CIDR blocks. Since the
      endpoint is user-configurable and may be any host on their own network, an
      allowlist of specific addresses would break the moment they point it
      somewhere else.

      This does not weaken anything else. Every public provider - Scryfall,
      TCGdex, YGOPRODeck, MTGStocks, GitHub, JustTCG - is HTTPS, and permitting
      cleartext does not stop Android validating their certificates. It only
      stops the platform rejecting the user's own server.
    -->
    <application
        android:label="Arcanum"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        android:usesCleartextTraffic="true"
        android:enableOnBackInvokedCallback="true"
        android:hardwareAccelerated="true">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>

    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
